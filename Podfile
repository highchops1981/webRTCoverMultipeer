# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'webRTCoverMultipeer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'GoogleWebRTC'
  pod 'Wrap'
  pod 'Unbox'

  # Pods for webRTCoverMultipeer

  target 'webRTCoverMultipeerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'webRTCoverMultipeerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
